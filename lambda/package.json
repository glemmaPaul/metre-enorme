{
  "name": "kanjer-lambda",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "esbuild handler.ts --bundle --platform=node  --loader:.html=text --loader:.ttf=text --target=node16 --outfile=dist/handler.js"
  },
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "@sparticuz/chromium": "^131.0.1",
    "csv-parse": "^5.6.0",
    "mustache": "^4.2.0",
    "puppeteer-core": "^23.10.4"
  },
  "devDependencies": {
    "esbuild": "^0.24.0"
  }
}
